

Running Web interact:

	Required Dependancies:
		 pynput
			to install:
			git clone https://github.com/moses-palmer/pynput.git
			regular python setup.py steps

		 selenium
			to install:
			   pip install selenium 

			to make it work with chrome in linux:
			   sudo apt install chromium-chromedriver

		 cv2:
			to install:
			   pip install numpy opencv-python pyautogui

			   I also needed: sudo apt-get install scrot



